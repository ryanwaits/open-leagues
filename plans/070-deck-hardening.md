# Plan 070: Deck hardening — the audit's follow-ups on 068/069

> **Executor instructions**: Follow step by step; verify everything; STOP conditions binding. Commit only in-scope files; do NOT push; leave `plans/` alone.
>
> **Drift check (run first)**: `git diff --stat 671cb31..HEAD -- 'src/routes/league/$leagueId/wire.tsx' 'src/routes/league/$leagueId/roster.tsx' src/components/deck.tsx src/components/lineup-board.tsx` → expected empty.

## Status
P2 · Effort S–M · Risk LOW (all point fixes inside the two deck consumers) · Planned at `c16a66d`, 2026-08-24

## Why this matters
The context deck shipped in 068/069 (Deck portal in Shell's `#deck-slot`; wire deck + sheet + continuous list; roster tracking tabs + trade cap). A post-land audit of exactly that diff confirmed five small defects: a dead Activity tab on non-hosted (Sleeper-synced) leagues, no keyboard focus rings on any new deck control, late load-more on the continuous list, scroll-tracking work running on desktop where the deck is invisible, and a Bench jump that can land on a collapsed bench. Each is a contained fix.

## Current state — verified excerpts (at `c16a66d`)

### `src/routes/league/$leagueId/roster.tsx`
- Tracking effect (~84–105): window scroll listener, rAF-throttled, `for (const name of DECK_SECTIONS) { const el = document.querySelector(`[data-deck-sec="${name}"]`); if (el && el.getBoundingClientRect().top <= 90) current = name; }` → `setActiveSec(current)`. Attached unconditionally — runs on desktop too, where the deck's nav is `md:hidden`.
- `jumpToSection(name)` (~107–114): querySelector + `window.scrollTo({ top: … - 76, behavior: "auto" })`. No collapsed-bench handling.
- Deck tab buttons (~377–390): `className={cn("h-8 rounded-pill px-3 text-[13px] font-medium", activeSec === name ? "bg-fg text-bg" : "text-faint")}` — no focus-visible classes. Trade cap Link (~393–399): `inline-flex h-9 shrink-0 items-center rounded-pill bg-fg px-3.5 text-[13px] font-medium text-bg` — none either.
- Activity anchor (~506): `{league.data.hosted ? (<section className="rounded-xl bg-surface ring-card" data-deck-sec="Activity"> … Waivers …` — the Trades section (~544) is also hosted-gated; the "Your moves" section (~599) renders for EVERY league and has no attr. So when `hosted` is false (real case — `src/lib/data/sleeper.server.ts` produces such leagues) the Activity chip never activates and its jump no-ops.

### `src/components/lineup-board.tsx`
Bench toggle button (~358): `<button type="button" aria-expanded={benchShown} data-deck-sec="Bench" onClick={() => setBenchOpen((v) => !v)} …>`. `benchOpen` defaults open (`useState(!benchCollapsed)`, `benchCollapsed = false`, and roster.tsx never passes it) — collapsed bench happens only after a manual toggle, but then the deck's Bench jump lands on a shut drawer. Do not edit this file (its one allowed attr already landed).

### `src/routes/league/$leagueId/wire.tsx`
- Deck ☰ button (~199–206) and ＋ button (~226–232): no focus-visible classes. Local `Chip` (~453–473): `h-9 rounded-sm px-3 font-mono text-xs` + active/idle backgrounds — no focus style (pre-existing, also used by the desktop in-flow controls and the sheet).
- Sentinel IO (~142–153): `new IntersectionObserver((entries) => { if (entries[0]?.isIntersecting) setVisible(...) })` — no `rootMargin`, so +25 loads only once the 1px sentinel is already on screen (visible pop at the bottom).
- Warm hook (~132–135): `useWarmRosterProfiles(leagueId, pageRows.map((p) => p.player_id))`. On phones the pager is gone, so this always warms page 1's ten rows while the list shows up to all rows. The hook (`src/lib/data/player-view.ts:81`) slices to 16 ids internally and keys its effect on `playerIds.join("\0")` — so pass a STABLE first-16 list on phones, not `shownRows` (whose growing key would restart the timers on every +25).

### Conventions
Focus ring recipe (see `src/components/ui/button.tsx:11` and matchup-board): `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-deep` (use `-outline-offset-2` where an offset ring would clip against a tight container). Tokens/utilities only; `cn()` from `@/lib/utils`; zero easing on state changes; commits imperative, no AI attribution, no plan/sprint words. Never edit `src/routeTree.gen.ts`, auth/engine/grok do-not-edit lists.

## Commands / gate
`bun run typecheck` → 0 · scoped `bunx biome check` on touched files → 0 new · fresh dir `PGLITE_DATA_DIR=/tmp/claude-501/pglite-070 bun test src scripts` → baseline 352 pass + 1 pre-existing flaky `import.meta.glob` error (lineup-cache.test.mjs); no NEW failure names · `bun run build:dev` → 0. QA via `~/.bun/bin/agent-browser` (Bash sandbox DISABLED; viewport via `set viewport 390 844`; login http://localhost:8080/login prefilled; league `lg_65h3kyr5up`); `close --all` when done.

## Scope
**In scope**: `src/routes/league/$leagueId/wire.tsx`, `src/routes/league/$leagueId/roster.tsx`, `src/skin/skin.test.mjs` (extend the two existing deck tests).
**Out of scope**: `deck.tsx`, `shell.tsx`, `lineup-board.tsx`, `player-view.ts` (the warm hook itself), `TablePager`, tokens/styles.css, all other routes, `plans/`.

## Steps

### Step 1: Activity works on every league (roster.tsx)
The anchor follows the league: keep `data-deck-sec="Activity"` on the Waivers section when hosted; when not hosted, the "Your moves" section carries it instead. Concretely: on the Waivers `<section>` leave the attr as is (it only renders when hosted); on the "Your moves" `<section>` add `data-deck-sec={league.data.hosted ? undefined : "Activity"}`. (React drops undefined-valued attrs, so hosted leagues still have exactly one Activity anchor — verify in the DOM.)
**Verify**: typecheck 0; hosted league in the browser: `document.querySelectorAll('[data-deck-sec="Activity"]').length` → 1 and it is the Waivers section.

### Step 2: Bench jump opens the bench (roster.tsx)
In `jumpToSection`, before scrolling: if the target element's `aria-expanded` attribute is `"false"`, `el.click()` first (the Bench anchor IS the toggle button), then perform the scroll inside a `requestAnimationFrame` so the expanded layout has committed. Type the querySelector as `HTMLElement` for `.click()`. Other sections have no `aria-expanded`, so they take the direct path.
**Verify**: 390 — collapse the bench by hand, tap the Bench tab: bench expands AND lands under the header.

### Step 3: Tracking is phone-only (roster.tsx)
Copy the 8-line `useIsPhone` matchMedia hook from wire.tsx (~69–81) into roster.tsx (duplication is the documented convention at ≤2 copies; a third copy is the extraction trigger — see maintenance note). Gate the tracking effect: `useEffect(() => { if (!isPhone) return; … }, [isPhone])` — listener attached only on phones; desktop attaches nothing.
**Verify**: 1024 — scroll the page; React DevTools or a temporary log confirms no `activeSec` churn (or simply: no listener via `getEventListeners(window)` in DevTools is optional — a code-level check that the effect early-returns is acceptable). 390 — tracking still flips chips.

### Step 4: Focus rings on every deck control
- roster.tsx: add the focus recipe to the three tab buttons and the Trade cap Link.
- wire.tsx: add it to the deck's ☰ and ＋ buttons, and to the local `Chip` button (one place — fixes deck chips, sheet chips, and desktop chips together).
Use `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-deep`; inside the segmented track use `-outline-offset-2` if the offset ring clips.
**Verify**: keyboard-Tab at 390 and 1024 reaches each control with a visible ring; no visual change on tap/touch.

### Step 5: The list loads ahead of the thumb (wire.tsx)
Give the sentinel IO `{ rootMargin: "600px 0px" }` so the next 25 rows mount before the user reaches the bottom.
**Verify**: 390 — flick-scroll the wire; the footer count advances before the end of the list is on screen (no bottom stall).

### Step 6: Warm what phones actually show (wire.tsx)
`useWarmRosterProfiles(leagueId, (isPhone ? rows.slice(0, 16) : pageRows).map((p) => p.player_id))` — phones warm the first 16 of the FULL filtered list (stable key, no timer restarts as `visible` grows); desktop keeps per-page warming.
**Verify**: typecheck 0; desktop paging still triggers warming per page (behavior unchanged at 1024).

### Step 7: Gate + tests
Extend skin.test.mjs — in `"the wire's context lives in the deck on phones"` add: wire.tsx matches `/rootMargin/`; in `"my team's sections answer to the deck"` add: roster.tsx matches `/aria-expanded/` (the bench-jump path) and does NOT match nothing vacuous — keep asserts meaningful. `bun test src/skin` → all pass. Full gate + screenshots: 390 light (deck focus ring via keyboard, bench-jump-after-collapse, deep scroll) + 1024 sanity.

## Done criteria
- [ ] typecheck 0; build:dev 0; fresh-dir tests no new failure names; skin tests pass incl. extended asserts
- [ ] Non-hosted path: "Your moves" carries the Activity anchor when hosted is false (code-verified; browser-verified if a non-hosted league is reachable, else note it)
- [ ] 390: bench jump expands a collapsed bench; list pre-loads ahead; every deck control shows a keyboard focus ring
- [ ] 1024: no scroll listener attached; desktop visuals byte-identical
- [ ] `git status` clean outside scope

## STOP conditions
- Drift on excerpts.
- The conditional Activity attr renders two anchors on a hosted league (React not dropping undefined) → STOP and report rather than restructuring.
- `el.click()` on the bench toggle causes any side effect beyond expanding (check its onClick — it only flips `benchOpen`).

## Maintenance notes
- Third `useIsPhone` copy now exists (player-sheet, wire, roster) — extraction to `src/lib/` is warranted the NEXT time a consumer appears; noted, not done here (out of scope).
- Deferred by the audit, on purpose: 44px tap targets on deck controls (36px + the deck row's padding is acceptable slop; revisit only on real misses), conditional `pb-36` padding (068 accepted the flat bump), `aria-pressed` → tablist semantics (segmented-nav pattern is defensible), shared deck-tab extraction (waits for the third deck consumer).

## Git workflow
`main`; one commit, e.g. `fix(deck): every league, every thumb — activity anchor, focus rings, eager list`. Do NOT push.
