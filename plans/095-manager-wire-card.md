# Plan 095: Monday wire card

Manager lab slice 3. Planned at `400a7dc`. Depends on 094.

## Done when

- `getWireCard(leagueId, rosterId)` returns remaining FAAB, call (add+bid band or no-move), receipt id
- Source is `spec` when frozen, else named `quantile`
- `getAdviceReceipt` expands the why
- Skill `open-leagues-wire` — no `addDrop`

SDIFFL demo: league `1312215005088739328`, roster 1.

## Out

Cron, push, autopilot, lineup writes, UI page
